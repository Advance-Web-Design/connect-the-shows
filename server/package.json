{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "npm run build:client && next build",
    "build:client": "cd ../client && npm install --include=dev && npm run build && node ../server/scripts/copy-client.js",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "firebase": "^11.8.1",
    "memory-cache": "^0.2.0",
    "next": "^14.2.0",
    "node-cache": "^5.1.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "eslint": "^9",
    "eslint-config-next": "15.3.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
